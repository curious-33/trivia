import styles from "./progressbar.module.scss";

const ProgressBarComponent = () => {
  return (
    <div className={styles.progressBarComponent}>ProgressBarComponent</div>
  );
};

export default ProgressBarComponent;
